apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
spec:
  healthCheck:
    type: HTTP
    requestPath: /
    port: 8080
  timeoutSec: 30
  connectionDraining:
    drainingTimeoutSec: 10
